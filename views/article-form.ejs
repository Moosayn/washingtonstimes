<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= article ? 'Edit' : 'New' %> Article - Admin</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
  <header class="admin-header">
    <div class="container">
      <h1><%= article ? 'Edit' : 'New' %> Article</h1>
      <nav>
        <a href="/admin-dashboard-secure-x9k2p7q">← Back to Admin</a>
      </nav>
    </div>
  </header>

  <main class="container admin-main">
    <form action="/admin-dashboard-secure-x9k2p7q/article" method="POST" enctype="multipart/form-data" class="article-form">
      <% if (article) { %>
      <input type="hidden" name="id" value="<%= article.id %>">
      <input type="hidden" name="existingImage" value="<%= article.image %>">
      <% } %>

      <div class="form-group">
        <label for="title">Title *</label>
        <input type="text" id="title" name="title" value="<%= article ? article.title : '' %>" required>
      </div>

      <div class="form-group">
        <label for="category">Category *</label>
        <select id="category" name="category" required>
          <option value="Politics" <%= article && article.category === 'Politics' ? 'selected' : '' %>>Politics</option>
          <option value="World" <%= article && article.category === 'World' ? 'selected' : '' %>>World</option>
          <option value="Business" <%= article && article.category === 'Business' ? 'selected' : '' %>>Business</option>
          <option value="Technology" <%= article && article.category === 'Technology' ? 'selected' : '' %>>Technology</option>
          <option value="Sports" <%= article && article.category === 'Sports' ? 'selected' : '' %>>Sports</option>
          <option value="Entertainment" <%= article && article.category === 'Entertainment' ? 'selected' : '' %>>Entertainment</option>
        </select>
      </div>

      <div class="form-group">
        <label for="author">Author</label>
        <input type="text" id="author" name="author" value="<%= article ? article.author : 'Staff Writer' %>">
      </div>

      <div class="form-group">
        <label for="image">Featured Image</label>
        <input type="file" id="image" name="image" accept="image/*">
        <% if (article && article.image) { %>
        <div class="current-image">
          <p>Current image:</p>
          <img src="<%= article.image %>" alt="Current" style="max-width: 200px;">
        </div>
        <% } %>
      </div>

      <div class="form-group">
        <label for="content">Content (Markdown supported) *</label>
        <textarea id="content" name="content" rows="20" required><%= article ? article.content : '' %></textarea>
        <small>You can use Markdown formatting. Example: **bold**, *italic*, # Heading, - List item</small>
      </div>

      <div class="form-actions">
        <button type="submit" class="btn-primary">Save Article</button>
        <a href="/admin-dashboard-secure-x9k2p7q" class="btn-secondary">Cancel</a>
      </div>
    </form>
  </main>
</body>
</html>
